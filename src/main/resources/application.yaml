ktor:
  development: "$KTOR_DEVELOPMENT:true"
  application:
    modules:
      - app.umcu.api.ApplicationKt.module
  deployment:
    port: 8080
postgres:
  db: "$POSTGRES_DB:"
  user: "$POSTGRES_USER:"
  password: "$POSTGRES_PASSWORD:"
